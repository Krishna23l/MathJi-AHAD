<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MathJi-AHAD | Education Portal</title>
    <style>
        body, html { margin: 0; padding: 0; height: 100%; font-family: sans-serif; overflow: hidden; background: #0b101b; color: white; }
        #rainCanvas { position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -1; }
        .main-container { display: flex; justify-content: center; align-items: center; height: 100vh; }
        .glass-box { background: rgba(255, 255, 255, 0.1); backdrop-filter: blur(15px); padding: 40px; border-radius: 20px; border: 1px solid rgba(255, 255, 255, 0.2); box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.5); text-align: center; width: 350px; }
        input { width: 100%; padding: 12px; margin: 10px 0; border-radius: 8px; border: none; outline: none; }
        button { width: 100%; padding: 12px; background: #00d2ff; color: black; border: none; border-radius: 8px; cursor: pointer; font-weight: bold; transition: 0.3s; }
        button:hover { background: #00a8cc; }
        #teacher-page { display: none; padding: 40px; overflow-y: auto; height: 100vh; text-align: left; width: 85%; margin: auto;}
        .teacher-header { display: flex; align-items: center; gap: 20px; margin-bottom: 30px; background: rgba(255,255,255,0.05); padding: 20px; border-radius: 15px; }
        .course-card { background: rgba(0,0,0,0.4); padding: 20px; border-radius: 10px; margin-top: 15px; border-left: 5px solid #00d2ff; }
    </style>
</head>
<body>
    <canvas id="rainCanvas"></canvas>
    <div id="login-section" class="main-container">
        <div class="glass-box">
            <h1 style="color: #00d2ff;">MathJi-AHAD</h1>
            <p>Student Registration</p>
            <input type="text" id="user" placeholder="Create Username">
            <input type="password" id="pass" placeholder="Create Password">
            <button onclick="showDashboard()">Login & Explore</button>
        </div>
    </div>
    <div id="teacher-page">
        <div class="teacher-header">
            <img src="https://i.pravatar.cc/100?u=math" style="border-radius: 50%; border: 3px solid #00d2ff;">
            <div>
                <h2>Instructor: Mr. Ahad</h2>
                <p>Mathematics Specialist | Experience: 5+ Years</p>
                <p>Email: ahad@mathji.com</p>
            </div>
        </div>
        <h3 style="color: #00d2ff;">Your Courses:</h3>
        <div class="course-card">
            <h4>1. Basic Algebra Mastery</h4>
            <p>বীজগণিতের কঠিন সমস্যার সহজ সমাধান শিখুন এখানে।</p>
            <button style="width: 120px;">Watch Now</button>
        </div>
        <div class="course-card">
            <h4>2. Geometry for Beginners</h4>
            <p>জ্যামিতির সকল চিত্র এবং সূত্র নিয়ে বিশেষ কোর্স।</p>
            <button style="width: 120px;">Watch Now</button>
        </div>
        <br>
        <button onclick="location.reload()" style="background: #ff4b2b; color: white; width: 100px;">Logout</button>
    </div>
    <script>
        const canvas = document.getElementById('rainCanvas');
        const ctx = canvas.getContext('2d');
        canvas.width = window.innerWidth; canvas.height = window.innerHeight;
        const raindrops = [];
        for (let i = 0; i < 150; i++) {
            raindrops.push({ x: Math.random() * canvas.width, y: Math.random() * canvas.height, length: Math.random() * 20 + 10, speed: Math.random() * 5 + 5 });
        }
        function drawRain() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            ctx.strokeStyle = 'rgba(174, 194, 224, 0.5)'; ctx.lineWidth = 1;
            raindrops.forEach(drop => {
                ctx.beginPath(); ctx.moveTo(drop.x, drop.y); ctx.lineTo(drop.x, drop.y + drop.length); ctx.stroke();
                drop.y += drop.speed; if (drop.y > canvas.height) drop.y = -drop.length;
            });
            requestAnimationFrame(drawRain);
        }
        drawRain();
        function showDashboard() {
            const u = document.getElementById('user').value;
            const p = document.getElementById('pass').value;
            if (u && p) {
                document.getElementById('login-section').style.display = 'none';
                document.getElementById('teacher-page').style.display = 'block';
                document.body.style.overflow = 'auto';
            } else { alert("Please fill in Username and Password!"); }
        }
    </script>
</body>
</html>